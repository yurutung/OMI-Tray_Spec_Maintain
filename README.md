# OMI-Tray_Spec_Maintain

1. frontend
    ```
    npm install
    npm run build
    ```

2. backend
    ```
    npm install
    npm run build
    npm start
    ```

3. electron
    ```
    npm install
    npm start
    ```